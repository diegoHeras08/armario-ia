// Tipos de navegacion local entre pantallas (sin React Navigation).

// Identificador de cada pantalla disponible en la primera fase.
export type NombrePantalla =
  | 'dashboard'
  | 'armario'
  | 'altaPrenda'
  | 'tryOn'
  | 'historial';